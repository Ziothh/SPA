export type SubtaskData = {
    id: number,
    title: string,
    isCompleted: boolean
}

interface Props {
    
}


const Subtask: React.FC<Props> = ({}) => {
    return (
        <div>
            
        </div>
    )
}


export default Subtask